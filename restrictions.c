#ifndef restrictions_c
#define FILE_results_c


#include "header.h"  // Para ter acesso às declarações
#include "global.h"

#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

/*struct _pq {Item * queue; int size; int free;};

PQ *PQinit(unsigned Size) {
 PQ * Res=NULL;
 Res = (PQ *) malloc(sizeof(PQ));
Res->queue = (Item *) malloc(Size * sizeof(Item));
 Res->size = Size; Res->free = 0;
return Res;
}
*/
//void PQinsert(PQ * PQueue, Item I){
 /* elemento inserido sempre na primeira posição livre */
// if ((PQueue->free+1) < PQueue->size)
//PQueue[PQueue->free++] = I;
/* else some error 
//}

BOOL IsEmpty(PQ * PQueue) {
 return PQueue->free == 0 ? TRUE : FALSE;
}


//restrictions A para ligações diretas

//restrict_A1 restrição veículo

//restrict_A2 tempo máximo da viagem por ligação

//restrict_A3 custo máximo da viagem por ligação


//restrictions B para total de ligações

//restriction_B1 tempo máximo da viagem total

//restriction_B2 custo máximo da viagem total

*/
#endif
